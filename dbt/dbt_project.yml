name: 'stock_pulse'
version: '1.0.0'
config-version: 2

profile: 'stock_pulse'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  stock_pulse:
    +materialized: view